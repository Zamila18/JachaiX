import { ModuleShell } from "@/components/shared/ModuleShell";

export function ModeratorPageView() {
  return (
    <ModuleShell
      label="Moderator"
      title="Reviewer governance and override controls"
      summary="Moderation controls and policy actions will be connected to audit APIs later."
      state="Future Integration"
    />
  );
}
