import { ModuleShell } from "@/components/shared/ModuleShell";

export function ChatPageView() {
  return (
    <ModuleShell
      label="Investigator Chat"
      title="Conversational evidence exploration"
      summary="Interactive chat with contextual claim retrieval is planned after core flow hardening."
      state="Future Integration"
    />
  );
}
