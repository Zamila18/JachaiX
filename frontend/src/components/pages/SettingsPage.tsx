import { ModuleShell } from "@/components/shared/ModuleShell";

export function SettingsPageView() {
  return (
    <ModuleShell
      label="Settings"
      title="Platform configuration and environment controls"
      summary="Feature flags, model settings, and policy configuration UI are prepared for integration."
      state="Future Integration"
    />
  );
}
